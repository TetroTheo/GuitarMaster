class_name NotePackage
extends Resource

## a duration of 1.0 is equivalent to one quarter note
@export var duration : float
@export var notes : Array[Note]

# note that this structure works for rests, too!
